# Copy-paste the code from 'Collisions' here, then modify it so that when two balls 'collide', the smaller of the two
# (i.e., smaller radius) is removed from the window. BUT the remaining (larger) ball is increased (i.e., its radius is
# increased by as much as the smaller\'s radius.

